#include <stdio.h>
#include <stdlib.h>
#include "arvore-b.h"

int main()
{
    int item, ch;
    
    inserir(50);
    inserir(9);
    inserir(1);
    inserir(11);
    inserir(15);
    procura(raiz);
}